# Performance Validation

Date: 2026-07-22

Dataset: `tmp/performance-backup-20000.json` generated by `pnpm perf:fixture` (4 workspaces, 20k tasks total, ~5k in the default workspace snapshot).

## Wave 10 notes (2026-07-22)

- FUN-008: backup schema **v3** + `{stem}_attachments/` sidecar for managed attachment binaries; import restores into app-managed storage before `importBackup`.
- FUN-001 residual: auto-backup `retentionCount` / `retentionDays` cleanup via Rust `cleanup_auto_backups`; v3 `clientPreferences.autoBackup` round-trips interval/retention (folder stays device-local).
- ARC-010 / PERF-005: [`DESKTOP_VALIDATION.md`](DESKTOP_VALIDATION.md) checklist expanded (sidecar + retention). Interactive Tauri + OS file-dialog / tray / 20k desktop feel remain **blocked** in this agent session — do not treat as a passed desktop release gate.

## Wave 5 notes (2026-07-22)

- PERF-002 Tier-2: SqlRepository folder CRUD, `updateWorkspace` / `restoreWorkspace`, and recurring create / template-update / disable use `commitCache` delta patches.
- Conformance suite extended for folder / workspace update / recurring (Local + mock Sql).
- Desktop UI checklist below remains blocked in this agent session: no interactive Tauri + OS file-dialog import can be driven from the coding agent. Fixture and data-layer automation are green; do not treat this as a passed desktop release gate.

## Automated Checks

- `pnpm test`: passed — 30 files, 238 tests (2026-07-22 Wave 10).
- `pnpm build`: passed — main JS ~252.5 kB (budget 500 kB).
- `pnpm perf:fixture`: passed — settings include `defaultSavedViewId`; `parseBackupPayload` gate green; fixture remains v2-compatible.
- `pnpm perf:runtime`: passed — LocalRepository 2k-task budgets.
- `pnpm test:e2e`: passed — 12 smoke tests.
- `pnpm perf:sqlite` (LocalRepository + fixture, representative): load / loadTaskPage / toggle / saveSettings P95 under documented budgets.
- `cd src-tauri && cargo fmt` / `clippy --locked -D warnings` / `test --locked` / `check --locked`: passed (Wave 10; 20 Rust lib tests including sidecar export + auto-backup cleanup).

## Manual Desktop Checks

Blocker (2026-07-22 Wave 10): agent environment cannot complete Settings → Import JSON with a native file picker or exercise tray/floating-window UX. Run locally with `pnpm tauri dev` and import `tmp/performance-backup-20000.json`, then update these rows. Also exercise attachment sidecar export/import and auto-backup retention once.

- Import 20k task backup: **blocked** (no interactive Tauri session in agent).
- Switch Home, Overview, Projects, Workspaces, Reminder Center, Settings: **blocked**.
- Home long list uses load more: **blocked**.
- Home text search on large list (FTS decision input): **blocked** — no desktop search timing available.
- Workspace existing-task picker loads on open: **blocked**.
- Workspace floating window long list uses load more: **blocked**.
- Open task detail from large list: **blocked**.
- After S1: complete a task on Overview after load-more — list should not jump back to only the first page: **blocked**.
- Attachment sidecar round-trip on desktop: **blocked** (unit/Rust covered; dialog UX not driven).
- Auto-backup retention prune on desktop: **blocked** (Rust unit covered).

## FTS / PERF-004 decision (Wave 5)

**Decision: do not implement SQLite FTS5 in this wave or as an immediate follow-up.**

Rationale:

- LocalRepository 20k `loadTaskPage` P95 stayed far under budget (~3.5 ms vs 250 ms) in automation.
- No Tauri + SQLite desktop search timing was obtained this session, so there is no evidence that `%query%` is a user-visible bottleneck.
- Short-term mitigation remains paged queries (`loadTaskPage`) and non-text filter indexes; revisit FTS5 only after a real desktop import + Home search pass records P50/P95 or clear jank.

## Notes

This file is a validation log. Automated / data-layer rows are filled; desktop UI rows are explicitly **blocked**, not claimed passed.
